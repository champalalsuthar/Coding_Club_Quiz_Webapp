// import { QuizData } from "../QuizData";

// export const getQuizByTitle = (title) => {
    // Filter QuizData based on the given title
    // const quiz = QuizData.filter((quizItem) => quizItem.tittle.toLowerCase() === title.toLowerCase());

    // Return the filtered quiz or null if no match is found
    // return quiz.length > 0 ? quiz[0] : null;
// };
